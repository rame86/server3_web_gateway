# /frontend/nginx.conf
server {
    listen 80;
    
    location / {
        root   /usr/share/nginx/html;
        index  index.html;
        # SPA 라우팅 핵심: 요청한 URI가 없으면 index.html을 반환
        try_files $uri $uri/ /index.html;
    }
}