# /frontend/nginx.conf
server {
    listen 80;           
    root /usr/share/nginx/html;   
    index index.html;
    location / {
        # SPA 라우팅을 위한 설정
        try_files $uri $uri/ /index.html;
    }
}
